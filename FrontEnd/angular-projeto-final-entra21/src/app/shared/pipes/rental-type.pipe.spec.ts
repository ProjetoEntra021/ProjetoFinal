import { RentalTypePipe } from './rental-type.pipe';

describe('RentalTypePipe', () => {
  it('create an instance', () => {
    const pipe = new RentalTypePipe();
    expect(pipe).toBeTruthy();
  });
});
